@*aqui fiz minha primeira partial, que será pra diferenciar quando um usuário está logado ou não*@
@using Microsoft.AspNetCore.Identity;
@using GerenciadorAlunosV2.Models;
@inject SignInManager<UsuarioModel> signInManager;

<ul class="navbar-nav ms-auto">
    @if (signInManager.IsSignedIn(User))
    {
        <partial name="_LoginPartial" />
        <li class="nav-item">
            <a class="nav-link text dark" asp-controller="Usuario" asp-action="Logout">Logout</a>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text dark" asp-controller="Usuario" asp-action="Login">Login</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text dark" asp-controller="Usuario" asp-action="Registrar">Registrar</a>
        </li>
    }
</ul>